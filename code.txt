

int main() {


    if (!file.is_open()) {
        return 1;
    }

    stringstream buffer;
    string sourceCode = str();

    Parser parser;
    
    try {
        parse(); 
    }
    catch () {
    }

    return 0;
}